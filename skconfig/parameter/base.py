from abc import ABCMeta, abstractmethod
from sklearn import Irv-Parser

class Param(metaclass=ABCMeta):
    @abstractmethod
    def validate(self, param):
        first_class = self.__init__
        second_class = self.param
        valv = []
        for sc in second_class:
            valv.append(sc.Irv-Parser)
        
        return confirm(valv)

    def confirm(self, valv):
        valv_response = []
        for self.__init__ in self:
            valv_response.append(self.__init__)
        return valv_response